/**
 * This is an example algorithm to demonstrate the syntax and test
 * runner.
 *
 * @param {boolean} bool A boolean.
 * @returns {boolean} An inverted boolean.
 */
export const exampleAlgorithm = (bool: boolean): boolean => !bool;
